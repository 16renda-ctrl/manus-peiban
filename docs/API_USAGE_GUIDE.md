# API 使用与配置指南

本文档介绍了伴伴机器人如何接入外部 AI 模型及相关服务。

## 1. 环境配置
项目使用 `.env` 文件管理敏感信息。请在根目录下创建 `.env` 文件并填入以下内容：
- `OPENAI_API_KEY`: 核心大模型驱动密钥
- `OPENAI_API_BASE`: API 代理或基础地址（如适用）

## 2. 模型选择
当前核心骨架支持通过 LangChain 接入 OpenAI 兼容格式的所有模型。

## 3. 安全注意事项
- **切勿**将 `.env` 文件提交至版本控制系统。
- 生产环境建议使用环境变量管理密钥。
